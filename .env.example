# Copy this file to .env for local development
# DO NOT commit .env to git

# Admin password for managing teams (change this!)
ADMIN_PASSWORD=masters2026

# Port (Railway/Render will set this automatically)
PORT=3000

# Path to SQLite database file
# For Railway: set this to /data/pool.db if you mount a volume at /data
DB_PATH=pool.db
